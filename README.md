# Simmer 🍲

Personal recipe-video library: share a cooking video from Instagram or TikTok,
Claude turns the caption into a recipe card, and the app lets you browse,
search, and watch everything you've saved.

- `docs/` — the web app (hosted on GitHub Pages)
- `supabase/` — database schema + the edge function (ingest, extraction, API)
